<?php
// Read the cyphertext

// Separate out the encrypted message, salt and nonce

// Recreate the hashed password (sodium_crypto_pwhash())

// Decrypt the message using the hashed password, nonce and encrypted message

// Print out the message

